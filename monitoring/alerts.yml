# Prometheus Alerts

      - alert: HighErrorRate
        expr: rate(http_requests_total{status=~"5.."}[5m]) > 0.05
        for: 2m
        labels:
          severity: critical
        annotations:
          summary: "Yüksək xəta nisbəti"
          description: "Xəta dərəcəsi 5%-dən yuxarıdır."
